/// A simple directed acyclic graph structure, internally similar to a [`LinkedList`].
/// 
/// This is an internal type and does not implement the [`Graph`] trait.
/// You may get better use out of higher level types that do implement [`Graph`].
/// This is still exposed for the use of advanced users.
/// 
/// [`LinkedList`]: alloc::collections::LinkedList
/// [`Graph`]: noodle_core::Graph
pub struct LinkedGraph {

}